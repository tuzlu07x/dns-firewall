{
  "name": "dns-firewall",
  "version": "0.1.0",
  "dependencies": {
    "dns-packet": "^5.6.1",
    "js-yaml": "^4.1.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "@types/dns-packet": "^5.2.1",
    "@types/js-yaml": "^4.0.9",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "ts-node ./src/index.ts",
    "build": "tsc index.ts"
  },
  "bin": {
    "dns-firewall": "./bin/dns-firewall"
  }
}
